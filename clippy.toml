# GENERATED by primitive-bypass-clippy@2.0.0 — do not edit.
# Source: package-manifest rows, `prohibits` entries.
# Adding a primitive's prohibition to that row denies its bypass here
# automatically; editing this file by hand is reverted by the next run.

disallowed-methods = [
  { path = "aws_sdk_s3::Client::from_conf", reason = "use object_storage::ObjectStore" },
  { path = "aws_sdk_s3::Client::new", reason = "use object_storage::ObjectStore" },
  { path = "openssl::symm::decrypt_aead", reason = "use sealed_value::open(envelope, key) or sealed_value::open_if_sealed(...)" },
  { path = "openssl::symm::encrypt_aead", reason = "use sealed_value::seal(plaintext, key)" },
  { path = "std::process::Command::new", reason = "use host_spawn::command(program), host_spawn::output(...) or host_spawn::output_with_env(...)" },
  { path = "std::time::SystemTime::now", reason = "use timing::unix_epoch_ms() or timing::unix_epoch_secs(); measure durations with timing::scoped(name) or timing::measure(name, f)" },
  { path = "tokio::process::Command::new", reason = "use host_spawn::command_async(program) or host_spawn::output_async(...)" },
  { path = "tokio::signal::ctrl_c", reason = "use signal_shutdown::install() and its ShutdownToken" },
  { path = "tokio::signal::unix::signal", reason = "use signal_shutdown::install() and its ShutdownToken" },
  { path = "tokio_postgres::Config::connect", reason = "use platform_db::connect (pooled, attributed connections)" },
  { path = "tokio_postgres::connect", reason = "use platform_db::connect (pooled, attributed connections)" },
]
